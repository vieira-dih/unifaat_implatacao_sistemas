# Lab 007 - Revisão Prática Individual

## Objetivos
- Consolidar conhecimentos das aulas 1-6
- Identificar e resolver lacunas no aprendizado
- Praticar troubleshooting em ambiente controlado
- Preparar-se para a Prova Teórico-Prática 1 (Aula 7.1)

## Metodologia da Revisão

Esta revisão seguirá uma abordagem **prática e individual**, onde cada aluno implementará um stack completo do zero, identificando e resolvendo problemas de forma autônoma.

### Estrutura da Sessão (2 horas)
1. **Diagnóstico inicial** (15 min) - Autoavaliação de competências
2. **Implementação guiada** (90 min) - Stack completo com checkpoints
3. **Troubleshooting dirigido** (30 min) - Resolução de problemas simulados
4. **Síntese e dúvidas** (15 min) - Consolidação e esclarecimentos

## Parte 1: Diagnóstico de Competências (15 min)

### Autoavaliação Rápida
Marque seu nível de confiança (1-5) em cada competência:

#### Docker (Aula 2)
- [ ] Criar Dockerfile funcional (1-2-3-4-5)
- [ ] Build de imagens personalizadas (1-2-3-4-5)
- [ ] Executar containers com parâmetros (1-2-3-4-5)
- [ ] Implementar healthchecks (1-2-3-4-5)

#### Docker Compose (Aula 3)
- [ ] Escrever docker-compose.yml (1-2-3-4-5)
- [ ] Configurar múltiplos serviços (1-2-3-4-5)
- [ ] Implementar volumes e persistência (1-2-3-4-5)
- [ ] Gerenciar redes entre containers (1-2-3-4-5)

#### Nginx (Aula 4)
- [ ] Configurar proxy reverso (1-2-3-4-5)
- [ ] Servir arquivos estáticos (1-2-3-4-5)
- [ ] Usar variáveis de ambiente (1-2-3-4-5)
- [ ] Interpretar logs (1-2-3-4-5)

#### Automação (Aula 5)
- [ ] Criar scripts de deploy (1-2-3-4-5)
- [ ] Implementar testes automatizados (1-2-3-4-5)
- [ ] Monitorar logs e métricas (1-2-3-4-5)
- [ ] Debuggar problemas (1-2-3-4-5)

**Foque nas competências com nota ≤ 3 durante a revisão!**

## Parte 2: Implementação Guiada (90 min)

### Cenário: Sistema de Tarefas (Todo App)

Implemente um sistema completo de gerenciamento de tarefas com:
- **Frontend**: Interface web simples
- **Backend**: API REST
- **Database**: PostgreSQL
- **Proxy**: Nginx
- **Monitoramento**: Healthchecks e logs

### Checkpoint 1: Estrutura Base (15 min)

```bash
# Criar estrutura do projeto
mkdir -p ~/revisao-lab007/{app,nginx,frontend,db}
cd ~/revisao-lab007

# Estrutura esperada:
# revisao-lab007/
# ├── docker-compose.yml
# ├── .env
# ├── app/
# │   ├── Dockerfile
# │   ├── package.json
# │   └── server.js
# ├── nginx/
# │   └── nginx.conf
# ├── frontend/
# │   └── index.html
# └── db/
#     └── init.sql
```

**✅ Verificação:** Estrutura de pastas criada

### Checkpoint 2: Backend API (25 min)

Crie `app/package.json`:
```json
{
  "name": "todo-api",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.0",
    "pg": "^8.8.0"
  }
}
```

Crie `app/server.js`:
```javascript
const express = require('express');
const { Pool } = require('pg');
const app = express();
const PORT = process.env.PORT || 3000;

app.use(express.json());

// Database connection
const pool = new Pool({
  host: process.env.DB_HOST || 'db',
  user: process.env.DB_USER || 'todo_user',
  password: process.env.DB_PASSWORD || 'todo_pass',
  database: process.env.DB_NAME || 'todo_db',
  port: 5432,
});

// Health check
app.get('/health', async (req, res) => {
  try {
    await pool.query('SELECT 1');
    res.json({ 
      status: 'healthy', 
      timestamp: new Date().toISOString(),
      database: 'connected'
    });
  } catch (error) {
    res.status(503).json({ 
      status: 'unhealthy', 
      error: error.message 
    });
  }
});

// Get all todos
app.get('/api/todos', async (req, res) => {
  try {
    const result = await pool.query('SELECT * FROM todos ORDER BY created_at DESC');
    res.json(result.rows);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

// Create todo
app.post('/api/todos', async (req, res) => {
  const { title, description } = req.body;
  try {
    const result = await pool.query(
      'INSERT INTO todos (title, description) VALUES ($1, $2) RETURNING *',
      [title, description]
    );
    res.json(result.rows[0]);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

// Update todo
app.put('/api/todos/:id', async (req, res) => {
  const { id } = req.params;
  const { completed } = req.body;
  try {
    const result = await pool.query(
      'UPDATE todos SET completed = $1 WHERE id = $2 RETURNING *',
      [completed, id]
    );
    res.json(result.rows[0]);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

app.listen(PORT, () => {
  console.log(`Todo API running on port ${PORT}`);
});
```

Crie `app/Dockerfile`:
```dockerfile
FROM node:16-alpine

RUN apk add --no-cache curl

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .

HEALTHCHECK --interval=30s --timeout=3s --start-period=10s --retries=3 \
  CMD curl -f http://localhost:3000/health || exit 1

EXPOSE 3000
CMD ["npm", "start"]
```

**✅ Verificação:** Backend API implementado

### Checkpoint 3: Database Setup (10 min)

Crie `db/init.sql`:
```sql
CREATE TABLE IF NOT EXISTS todos (
  id SERIAL PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  description TEXT,
  completed BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Dados de exemplo
INSERT INTO todos (title, description) VALUES 
('Estudar Docker', 'Revisar conceitos de containerização'),
('Configurar Nginx', 'Implementar proxy reverso'),
('Implementar Healthchecks', 'Adicionar monitoramento da aplicação');
```

**✅ Verificação:** Script de inicialização do banco criado

### Checkpoint 4: Frontend Interface (15 min)

Crie `frontend/index.html`:
```html
<!DOCTYPE html>
<html>
<head>
    <title>Todo App - Revisão Lab 007</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; background: #f5f5f5; }
        .container { max-width: 800px; margin: 0 auto; background: white; padding: 20px; border-radius: 8px; }
        .todo { border: 1px solid #ddd; padding: 15px; margin: 10px 0; border-radius: 4px; }
        .todo.completed { background: #f0f8f0; text-decoration: line-through; }
        .health { background: #e8f4f8; padding: 15px; margin: 20px 0; border-radius: 4px; }
        button { padding: 8px 16px; margin: 5px; border: none; border-radius: 4px; cursor: pointer; }
        .btn-primary { background: #007bff; color: white; }
        .btn-success { background: #28a745; color: white; }
        input, textarea { width: 100%; padding: 8px; margin: 5px 0; border: 1px solid #ddd; border-radius: 4px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Todo App - Revisão Lab 007</h1>
        
        <div class="health">
            <h3>Status do Sistema</h3>
            <button class="btn-primary" onclick="checkHealth()">Verificar Saúde</button>
            <div id="health-status"></div>
        </div>
        
        <div>
            <h3>Adicionar Nova Tarefa</h3>
            <input type="text" id="title" placeholder="Título da tarefa">
            <textarea id="description" placeholder="Descrição (opcional)"></textarea>
            <button class="btn-success" onclick="addTodo()">Adicionar</button>
        </div>
        
        <div>
            <h3>Minhas Tarefas</h3>
            <button class="btn-primary" onclick="loadTodos()">Carregar Tarefas</button>
            <div id="todos"></div>
        </div>
    </div>
    
    <script>
        async function checkHealth() {
            try {
                const response = await fetch('/health');
                const data = await response.json();
                document.getElementById('health-status').innerHTML = 
                    `<p><strong>Status:</strong> ${data.status}</p>
                     <p><strong>Database:</strong> ${data.database}</p>
                     <p><strong>Timestamp:</strong> ${data.timestamp}</p>`;
            } catch (error) {
                document.getElementById('health-status').innerHTML = 
                    `<p style="color: red;">Erro: ${error.message}</p>`;
            }
        }
        
        async function loadTodos() {
            try {
                const response = await fetch('/api/todos');
                const todos = await response.json();
                const todosHtml = todos.map(todo => 
                    `<div class="todo ${todo.completed ? 'completed' : ''}">
                        <h4>${todo.title}</h4>
                        <p>${todo.description || 'Sem descrição'}</p>
                        <small>Criado em: ${new Date(todo.created_at).toLocaleString()}</small>
                        <button class="btn-primary" onclick="toggleTodo(${todo.id}, ${!todo.completed})">
                            ${todo.completed ? 'Reabrir' : 'Concluir'}
                        </button>
                    </div>`
                ).join('');
                document.getElementById('todos').innerHTML = todosHtml;
            } catch (error) {
                document.getElementById('todos').innerHTML = 
                    `<p style="color: red;">Erro ao carregar tarefas: ${error.message}</p>`;
            }
        }
        
        async function addTodo() {
            const title = document.getElementById('title').value;
            const description = document.getElementById('description').value;
            
            if (!title) {
                alert('Título é obrigatório!');
                return;
            }
            
            try {
                const response = await fetch('/api/todos', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ title, description })
                });
                
                if (response.ok) {
                    document.getElementById('title').value = '';
                    document.getElementById('description').value = '';
                    loadTodos();
                }
            } catch (error) {
                alert('Erro ao adicionar tarefa: ' + error.message);
            }
        }
        
        async function toggleTodo(id, completed) {
            try {
                const response = await fetch(`/api/todos/${id}`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ completed })
                });
                
                if (response.ok) {
                    loadTodos();
                }
            } catch (error) {
                alert('Erro ao atualizar tarefa: ' + error.message);
            }
        }
    </script>
</body>
</html>
```

**✅ Verificação:** Interface frontend criada

### Checkpoint 5: Nginx Configuration (10 min)

Crie `nginx/nginx.conf`:
```nginx
events {
    worker_connections 1024;
}

http {
    upstream backend {
        server app:3000;
    }
    
    server {
        listen 80;
        server_name localhost;
        
        # Logs
        access_log /var/log/nginx/access.log;
        error_log /var/log/nginx/error.log;
        
        # Health check do Nginx
        location /nginx-health {
            access_log off;
            return 200 "nginx healthy\n";
            add_header Content-Type text/plain;
        }
        
        # Proxy para API
        location /api/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
        
        # Health check da aplicação
        location /health {
            proxy_pass http://backend/health;
        }
        
        # Servir frontend
        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }
    }
}
```

**✅ Verificação:** Configuração do Nginx criada

### Checkpoint 6: Docker Compose (15 min)

Crie `.env`:
```env
# Database
POSTGRES_DB=todo_db
POSTGRES_USER=todo_user
POSTGRES_PASSWORD=todo_pass

# Application
PORT=3000
DB_HOST=db
DB_USER=todo_user
DB_PASSWORD=todo_pass
DB_NAME=todo_db

# Nginx
NGINX_PORT=8080
```

Crie `docker-compose.yml`:
```yaml
version: '3.8'

services:
  db:
    image: postgres:13-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - todo_network

  app:
    build: ./app
    environment:
      - PORT=${PORT}
      - DB_HOST=${DB_HOST}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - todo_network

  nginx:
    image: nginx:alpine
    ports:
      - "${NGINX_PORT}:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./frontend:/usr/share/nginx/html
    depends_on:
      - app
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/nginx-health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - todo_network

volumes:
  db_data:

networks:
  todo_network:
    driver: bridge
```

**✅ Verificação:** Orquestração completa configurada

## Parte 3: Troubleshooting Dirigido (30 min)

### Cenário 1: Container não inicia (10 min)

**Problema simulado:** Remova uma dependência do package.json e tente subir o stack.

```bash
# Simular problema
sed -i 's/"pg": "^8.8.0",//g' app/package.json

# Tentar subir
docker-compose up -d --build

# Diagnosticar
docker-compose logs app
```

**Exercício:** Identifique e corrija o problema usando logs.

### Cenário 2: Conectividade entre serviços (10 min)

**Problema simulado:** Altere o nome do serviço de banco no docker-compose.yml.

```bash
# Simular problema
sed -i 's/db:/database:/g' docker-compose.yml

# Tentar subir
docker-compose up -d --build

# Diagnosticar
docker-compose logs app
```

**Exercício:** Identifique o problema de conectividade e corrija.

### Cenário 3: Proxy reverso não funciona (10 min)

**Problema simulado:** Altere a configuração do upstream no nginx.conf.

```bash
# Simular problema
sed -i 's/server app:3000;/server app:3001;/g' nginx/nginx.conf

# Recarregar nginx
docker-compose restart nginx

# Testar
curl -I http://localhost:8080/api/todos
```

**Exercício:** Diagnostique e corrija o problema do proxy reverso.

## Parte 4: Síntese e Preparação (15 min)

### Comandos Essenciais para a Prova

```bash
# Gestão do stack
docker-compose up -d --build
docker-compose ps
docker-compose logs service-name
docker-compose exec service command
docker-compose down -v

# Troubleshooting
curl -I http://localhost:port/endpoint
docker-compose exec app ping db
docker stats --no-stream
netstat -tlnp | grep port

# Verificação de arquivos
docker-compose config
nginx -t (dentro do container)
```

### Checklist Final de Preparação

#### ✅ **Competências Técnicas**
- [ ] Consigo criar stack completo do zero
- [ ] Sei diagnosticar problemas com logs
- [ ] Entendo conectividade entre containers
- [ ] Domino configuração de proxy reverso
- [ ] Implemento healthchecks funcionais

#### ✅ **Competências Comportamentais**
- [ ] Mantenho calma sob pressão
- [ ] Uso abordagem sistemática para problemas
- [ ] Gerencio tempo adequadamente
- [ ] Documento passos importantes
- [ ] Testo incrementalmente

### Dicas Finais para a Prova

1. **Leia todas as instruções** antes de começar
2. **Implemente incrementalmente** - teste cada parte
3. **Use logs para diagnosticar** problemas
4. **Mantenha backup** de comandos importantes
5. **Gerencie tempo** - 2 horas passam rápido
6. **Mantenha calma** - você já fez isso antes!

## Recursos de Apoio

- [TA007.md](TA007.md) - Conceitos teóricos da revisão
- [Lab006.md](../Aula006/Lab006.md) - Laboratório de referência
- Documentação oficial: Docker, Nginx, PostgreSQL
- Troubleshooting guides das aulas anteriores

## Próxima Aula
**Aula 007.1 - Prova Teórico-Prática 1**
- Cenário similar mas diferente
- Tempo: 2 horas + 15 min demonstração
- Avaliação individual obrigatória

---

**🎯 Objetivo:** Consolidar conhecimentos e ganhar confiança para a prova!